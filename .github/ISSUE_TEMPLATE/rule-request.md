---
name: 规则请求
about: 新增、修改或删除某条 .stignore 忽略规则
title: "[Rule] "
labels: ruleset
assignees: ''
---

## 请求类型

- [ ] 新增规则（现有规则集漏掉了某类噪音文件）
- [ ] 修改规则（现有规则写法有误，例如匹配不到或过于宽泛）
- [ ] 删除规则（现有规则会误伤需要同步的文件）

## 相关分类

<!-- 对应 .stignore 中的 20 个分类之一，或说明需要新建分类 -->

分类名称：

## 模式内容

<!-- 建议的模式，一行一条；请遵循 目录以 / 结尾、文件用 **/*.ext 前缀 的约定 -->

```text

```

## 理由

<!-- 这类文件为什么应该被忽略？占用多少空间？出现在什么场景？ -->

## 误伤评估（重要）

<!-- 该模式可能命中哪些"不应该被忽略"的文件？例如 *.lock 会误伤 Cargo.lock -->

可能误伤：

规避方式（如用 `!` 取反保留、改用 `(?i)` + 完整目录名）：

## 验证情况

- [ ] 已在 Syncthing Web 界面「忽略模式」预览中确认能命中目标文件
- [ ] 已确认没有命中需要同步的真实文件
- [ ] 已确认不与现有规则重复
