# 扫描工具生成的路径清单（含用户电脑隐私路径，不入库）
config/stignore-paths.json

# 替换工具产生的原文件备份
*.bak.*
config/*.bak.*

# 用户偏好配置（语言/主题，本地持久化，不入库）
config.json
*.mjs
_cnt.ps1

# Flutter pub get 日志（构建临时产物）
_pubget.log

# 本地临时校验脚本
__*_tmp.ps1
