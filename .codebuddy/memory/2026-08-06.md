# 2026-08-06

## 项目：SyncthingIgnorePatterns (GUI 工具 PowerShell 5.1 WinForms)
- 关键架构红线：脚本必须保持**纯 ASCII**，中文界面文案一律 `\uXXXX` 转义（Decode-Uni 还原），否则 GBK 重编码会破坏 UTF-8 中文导致解析失败。
- 并行扫描用 **runspace 线程池**（最多4线程），不可用 PowerShell 7 专有 `ForEach-Object -Parallel`。
- 项目真实 remote：`https://github.com/sutchan/Syncthing_Ignore_Patterns`（与本地文件夹名 SyncthingIgnorePatterns 不同）。
- 本会话所有 git/powershell 执行命令均被环境 skip，改动需用户本地手动提交。

## 当日改动时间线（版本 v1.4.0→v1.8.0）
1. **v1.5.0 扫描提速**：runspace 并行扫描 + `-Filter .stignore` 替代 `-Include`；统一日志函数 `Write-LogLine`（`Add-Log` 兼容别名）；修复旧版调用未定义 `Write-LogLine` 崩溃隐患。
2. **v1.6.0 乱码修复+版本地址**：英文字典 `enItem` 误写真实中文'中文'致乱码→改纯ASCII；底部状态栏显示版本号与可点击项目主页 LinkLabel。
3. **v1.7.0 卡顿修复**：`btnScan` 同步调用 `Start-ScanJob`→`EndInvoke` 阻塞 GUI 线程。改为后台 `[powershell]::Create().BeginInvoke()` + `Timer(100ms)` 轮询 DoEvents；抽无UI函数 `Invoke-ScanCore`。
4. **v1.8.0 备份轮转+README**：新增 `Limit-Backups -Base -Keep 3`（按 LastWriteTimeUtc 留最新3个删其余），Apply 中 `.stignore` 备份与清单备份后各调用一次；README 界面功能表格化、中英各加 Mermaid 工作流图、补"备份轮转≤3"说明。
5. **备份豁免源文件**：Apply 替换分支增加 `if ($full -eq $Source)` 判断，当被替换目标恰为标准规则源 `.stignore` 自身时，跳过 `Copy-Item` 备份与 `Limit-Backups`，仅写入内容（日志提示"不备份，源 .stignore"）。其他位置 `.stignore` 仍正常备份。
6. **v1.9.0 GUI 完善**：Apply 改后台 runspace+Timer（与 Scan 一致），进度条 Blocks 真实百分比；新增扫描摘要标签（启动自动加载清单数量、Scan/Apply 后刷新）；非预览非强制时 Apply 前弹确认框；新增「清空日志」按钮；Set-Busy 禁用新按钮；ApplyJob 通过 `$FormObj` 参数 + `form.Invoke` 跨线程更新进度/摘要。版本号（脚本头/$ScriptVersion/.stignore/README/openspec）统一升 v1.9.0。仍无法自动执行 PS 校验，需用户本地验证。
7. **v1.10.0 GUI 全面增强**：实施全部建议改进——停止按钮（$script:cancelFlag 软取消）、结果 ListBox（双击打开）、语言/主题持久化（config.json，加入 .gitignore）、拖拽填充（form.AllowDrop）、进度百分比文字（lblPct）、完成 MessageBox、主题切换（Apply-Theme 循环控件配色）、关于按钮（btnAbout）。Write-LogLine 已含 ScrollToCaret 自动滚动。版本统一升 v1.10.0。仍无法自动执行 PS 校验，需用户本地验证。注：取消为"软取消"——UI 立即恢复，后台 runspace 静默收尾（ApplyJob 无法跨作用域读 $script:cancelFlag）。
8. **修复 A + 必修三项（同日）**：A) Set-Busy 联动 $btnStop.Enabled（忙碌才可用），并绑定 lblPct 显隐。#1 真实进度：Start-ParallelScan 加 -FormObj，每收集完一个 root 经 form.Invoke 推真实百分比；Invoke-ScanCore 透传；btnScan 调用加 $form 参数；删假动画 scanPct。#2 取消一致：btnScan 取消分支在 EndInvoke 前 return，丢弃结果不写清单，日志改为"未写入清单"。#3 预览填列表：预览改走统一后台路径，仍填 lstResults/摘要，仅跳过 Set-Content。Start-ScanJob 成死代码保留。

## 图片处理
- image_gen 生成成功但未落盘（工作区无 png）。改用 **Mermaid 流程图**（GitHub 原生渲染、纯文本、契合纯ASCII哲学）替代二进制截图。

## 审查/规范/同步
- 审查修正中英文 README：合并重复"语言切换"条目、修正 Preview only 语义（默认直接扫描）、"清单记录 SHA256"→已过时（v1.3 起仅 size/lastWriteUtc）。
- 新增 `openspec/project.md` 与 `openspec/specs/stignore-gui/spec.md`。
- 所有文件版本号已同步至 v1.8.0（脚本头/`$ScriptVersion`/中英文 README 徽章/状态栏/CHANGELOG）。

## 验证
- 每次改动后均用 `Parser::ParseFile` 语法校验通过（SYNTAX_OK）；CJK_CHARS=0（无真实中文字符残留）。
