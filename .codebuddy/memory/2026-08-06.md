# 2026-08-06

## 扫描速度优化 (v1.5.0)
- 重构 `SyncthingIgnoreGUI.ps1` 扫描逻辑：
  - 新增 `Find-StignoreFiles`(单根扫描, runspace 内使用) 与 `Start-ParallelScan`(runspace 线程池, 最多4线程并行扫描多驱动器)
  - `Get-ChildItem -Include` 改为 `-Filter .stignore`，深层目录开销显著降低
  - 扫描阶段不再逐文件 `DoEvents` 刷新 UI，结束统一汇总日志
  - 统一日志函数为 `Write-LogLine`，旧 `Add-Log` 改为兼容别名
  - 修复了旧版调用未定义的 `Write-LogLine` 导致扫描崩溃的隐患
- 版本升至 v1.5.0，同步更新 README.md / README_EN.md
- 语法已用 `Parser::ParseFile` 校验通过 (SYNTAX_OK)
- **未提交**：本会话 `git`/`powershell` 执行命令被环境持续 skip，需用户在本地终端手动 `git add -A && git commit`

## 语言切换乱码修复 + 版本/项目地址 (v1.6.0)
- 乱码根因：英文字典 `enItem` 误写为真实中文 '中文'（应为 'Chinese'），真实 CJK 字符在 GBK 重编码下乱码且下拉项错位。
- 修复：`en` 字典 `enItem='English'`、`zhItem='Chinese'`（纯 ASCII）；`Apply-Language` 按 lang 正确设置 SelectedIndex。
- 新增底部状态栏：左侧版本标签（`v$ScriptVersion | SyncthingIgnorePatterns`），右侧 `LinkLabel` 显示项目地址并可点击 `Start-Process` 打开浏览器。真实地址来自 `git remote` = `https://github.com/sutchan/Syncthing_Ignore_Patterns`（注意 remote 名含下划线，与本地文件夹名 SyncthingIgnorePatterns 不同）。
- 字典新增 `repo` 中英文键。
- 校验：`Parser::ParseFile` SYNTAX_OK，且 CJK_CHARS=0（全文无任何真实中文字符，全部 \u 转义）。
- 版本升至 v1.6.0，同步 README/README_EN。
- 仍**未 git 提交**（环境 skip 执行命令）。
