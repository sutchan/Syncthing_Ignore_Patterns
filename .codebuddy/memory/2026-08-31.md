# 2026-08-31

## .stignore 规则集审查（v1.14.x，仅审查未改文件）

实测数据：16 个分类注释、225 条规则、无完全重复行。

关键缺陷（已用近似 Syncthing pattern→正则脚本验证）：
- `/node_modules/**` 为根锚定，嵌套 node_modules（monorepo 子包）不忽略。
- `**/.vscode/*` 只匹配直接子项，`.vscode/extensions/**` 等深层文件漏网；`!/.vscode/settings.json` 仅恢复根级。
- `**/.swp` / `**/.swo` 匹配不到 vim 真实交换文件（`.foo.swp`），应为 `**/*.swp` / `**/*.swo`。
- `**/vendor/` 含斜杠 → 不匹配同步根级 `vendor/`（只匹配子级）。
- `**/*.db` 误伤真实 SQLite 数据；`*.lock` 误伤 `Cargo.lock`。
- `**.stfolder/` 与文件内注释矛盾（注释警告忽略会破坏 folder marker，却仍忽略）；`**.stversions` 同理。
- 宽泛误伤：`**download/`、`**downloads/`、`**Cache*`、`**Internet*`、`**log/`、`**logs/`、`**metadata/`、`**packages/`、`**thumb/`。

文档不一致：
- README 徽章/正文「17 个分类」，实测 16（CHANGELOG v1.14.1 声称已对齐，实际未对齐）。
- `.stignore` 头 1.14.0 / 2026-08-06；脚本与 README 徽章 1.14.1（属设计上不同步，但 README 第 32 行说明文案「当前 v1.14.0，发布于 2026-08-07」已过时）。

缺口：缺 `**/target/`（Rust）、`**/bin/`、`**/obj/`、`**/dist/`、`**/build/`、`**/*.pyc`、`**/*.class`；建议忽略 GUI 自身备份 `**/.stignore.bak.*`、`**/stignore-paths.json.bak.*`。

环境坑：PowerShell 下 `node -e` 会吞 `$`，正则脚本必须写临时 `.mjs` 文件执行（已验证）。

## 后续：已按审查结论修订，版本 v1.14.1 → v1.15.0

改动文件：`.stignore`（重写，225→258 条规则，17 类）、`SyncthingIgnoreGUI.ps1`（头 + `$ScriptVersion`）、`config.json`、`README.md`、`README_EN.md`、`CHANGELOG.md`、`openspec/project.md`。

已修 P0 全部 7 项 + P1 宽泛规则 + P2 风格统一；新增第 17 类「构建产物与语言输出」（恰好补齐 README 徽章久存的 17 类缺口，徽章数字无需改）。

设计决策（下次勿回退）：
- 不忽略 `.stfolder/` 与 `.stversions`（保留 folder marker 与版本控制），已在文件内留注释说明。
- 不写通配 `**/*.db`；`*.lock` 用 `!` 反转保留 Cargo.lock/package-lock.json/pnpm-lock.yaml/yarn.lock/composer.lock/poetry.lock/go.sum/flake.lock。
- Syncthing `!` 取反为「后匹配优先」，故取反行必须紧跟在对应正向行之后。
- `**/vendor/` 可能不匹配同步根级 `vendor/`（Syncthing 中 `**` 未必覆盖零个路径段），故并列写 `/vendor/` 与 `**/vendor/`。
- 分类注释写成 `//Name`（无空格），横向说明写成 `// NOTE: ...`（有空格）——自研校验脚本靠此区分分类标题与说明文字。

未做（用户未要求）：未提交 git；未在本机执行 PowerShell 语法校验（脚本仅改版本字符串）。

## 后续 2：补全 cache/temp 过滤，v1.15.0 → v1.16.0

用户要求补全 cache / temp 等缓存临时目录过滤。新增 3 个分类（18/19/20），规则 258 → 285，分类 17 → 20。

关键设计（勿回退）：用 `(?i)` 前缀做大小写不敏感匹配 + 精确目录名（非前缀通配），如 `(?i)**/cache/`、`(?i)**/temp/`、`(?i)**/tmp/`。这样既覆盖 `Cache/`/`CACHE/`/`TMP/`，又不会误伤 `MyCacheFolder/`、`Template/`、`Tempura/` —— 这是对 v1.15.0 那轮「删除宽泛 `**Cache*`/`**Temp/`」的安全回归方案。

归类调整：把原 Media & Player Caches 里的 9 条 cache 规则（`.cache/`、`.thumbnails/`、`Cache/`、`Caches/`、`CacheStorage/`、`Code Cache/`、`GPUCache/`、`ShaderCache/`、`INetCache/`）移入新类，消除与新类的 5 条重复；`**/Temp/`、`**/.tmp/` 被 `(?i)` 规则覆盖后删除。

新类内容：
- 18 Cache & Temp Directories：`(?i)` 的 cache/.cache/caches/temp/tmp/.tmp/cachedata/thumbnails/.thumbnails/thumbnail/thumbs + `.eslintcache` `.sass-cache/` `.rollup.cache/` `*.tsbuildinfo`
- 19 Browser & Electron Storage Caches：CacheStorage/、Cache_Data/、CachedData/、CachedExtensions/、Code Cache/、GPUCache/、ShaderCache/、DawnCache/、D3DSCache/、INetCache/、Application Cache/、Local Storage/、Session Storage/、IndexedDB/、blob_storage/、Crashpad/、Crash Reports/
- 20 OS Temp & Cache Locations（根锚定）：/tmp/、/var/tmp/、/var/cache/、/private/tmp/、/private/var/folders/、/Windows/Temp/

同步：脚本头 + `$ScriptVersion`、`config.json`、README 中英（徽章 17→20、规则数 258→285、分类列表加 18/19/20、两条新提示）、CHANGELOG、openspec/project.md（含第 5 节「17 类」→「20 类」描述）。

`.stignore` 注释中的 em dash（U+2014）已全部替换为 ASCII `-`，文件现为纯 ASCII（与项目 GBK 乱码红线对齐）。

## 后续 3：精简 README 中英文文档，v1.16.0 → v1.16.1

用户要求「精简 readme」，两份 README 均由 ~250 行压到 ~110 行。

精简手法（后续文档维护沿用此结构）：
- 删目录、前置条件、验证生效、独立「示例」章节；示例合并进「自定义建议」的小代码块。
- 删重复内容：界面布局同时存在 mermaid 图 + ASCII 大图 + 功能表格三份，只保留功能表格；重复的「使用方式」段落合并到「批量同步工具」开头。
- 分类列表由 20 段长句改为紧凑三列表格（# / 分类 / 典型规则），每条示例从 ~10 个压到 4~6 个。
- 保留：徽章、特性四条、快速开始、语法表、分类表格、两条误伤提示、workflow mermaid、备份轮转说明。

版本同步：脚本头 `//Version` + `$ScriptVersion`、`.stignore` 头、README 徽章/正文版本引用、openspec/project.md（目录结构注记 + 第 7 节）、CHANGELOG。

顺带补漏：CHANGELOG.md 缺 v1.16.0 小节（当时只写进 openspec/project.md 第 7 节），已补。教训：本项目 CHANGELOG 与 openspec/project.md §7 是双副本，改版本须两处都写。

## 后续 4：创建 .github/ Community Health Files，v1.16.1 → v1.16.2

新建 9 个文件（**未创建 .github/readme.md**，符合全局禁止规则）：
- `CONTRIBUTING.md`：架构红线三条（纯 ASCII + \u 转义 / 单一自包含脚本优先于 200 行拆分 / PS 5.1 不用 PS7 语法）、规则集编写约定表、禁止事项（不忽略 .stfolder 与 .stversions、不写通配 *.db、*.lock 必须反转保留、不用宽泛前缀）、自检命令、版本同步清单、提交规范与 PR 清单
- `CODE_OF_CONDUCT.md`：Contributor Covenant v2.1 中文改编，举报渠道写 @sutchan 私下联系（无邮箱可用）
- `SECURITY.md`：只支持最新版本、走 Security Advisories 私有报告、3 工作日确认/7 工作日评估、提醒清单含本机绝对路径须脱敏、列出非安全范围事项
- `SUPPORT.md`：文档索引表 + 3 个 Issue 模板链接 + 5 条常见问题（STA 打不开、GBK 乱码、扫描慢、备份堆积、规则误伤）
- `PULL_REQUEST_TEMPLATE.md`：按「规则集 / GUI 脚本 / 版本文档」三类分组的检查清单
- `ISSUE_TEMPLATE/`：`bug_report.md`(label bug)、`rule-request.md`(label ruleset)、`feature_request.md`(label enhancement)、`config.yml`（blank_issues_enabled: false + 4 条 contact_links）

仓库事实（供后续引用）：origin = https://github.com/sutchan/Syncthing_Ignore_Patterns.git，owner `sutchan`，脚本内 `$RepoUrl` 同址。labels `bug`/`ruleset`/`enhancement` 需在仓库中实际创建才会生效（GitHub 不会自动建 label）。

**未创建 LICENSE 文件**：README 声明 MIT，但仓库根与 .github/ 均无 LICENSE 文件 —— 已在回复中提示用户，待其确认后再补（未擅自添加）。

## 后续 5：GUI 实时扫描状态显示，v1.16.2 → v1.17.0

改动文件：`SyncthingIgnoreGUI.ps1`（唯一代码改动）、`.stignore` 头、README.md / README_EN.md（徽章+GUI 功能条目）、CHANGELOG.md、openspec/project.md（目录结构注记 + §5 功能表 + §7）。

实现要点（勿回退）：
- 共享状态 `$script:scanState`（`[hashtable]::Synchronized` + 独立 `Lock` 对象 + `ArrayList::Synchronized` 的 Records）。扫描 runspace 写 Files/Last/Records，UI Timer（100ms）只读取 —— 不再用 `form.Invoke` 回调推进度，省去跨线程封送，也避开 Marquee 模式赋值 Value 抛 `InvalidOperationException`。
- Files 递增用 `Monitor` 加锁（多 runspace 并发写）；Done 只在编排线程写，无需锁。
- 新增 `lblStatus`（Name='lblStatus'，日志框 104→88 高腾位）单行显示 `已完成根数/总数 | 已找到 N | 最新命中路径 | mm:ss`；`Shorten-Path` 长路径截断（52 字符，头 27 + ... + 尾 22）。
- 命中即流式追加到 `lstResults`（`$script:scanRendered` 游标），摘要同步「正在扫描…已找到 N」；完成后仅在列表为空时才重建。
- 单根目录（总量未知）进度条转 `Style=Marquee`（`Set-Busy -Mode scan -Total $n`），lblPct 改显文件数；多根目录仍按根数真实百分比。Set-Busy $false 先切回 Blocks 再赋值 Value。
- Apply 侧：`$done++` 移到循环入口（原只在写入分支递增，跳过/一致/清理路径不计导致进度偏慢），进度回调改 `[Action[int,string]]` 同时更新 progress/lblPct/lblStatus。
- 新增双语键：statusIdle/statusPrep/statusScan/statusApply/scanning/statusScanDone/statusApplyDone/statusStopped（均 `\u` 转义，保持纯 ASCII）。

验证：PowerShell `Parser::ParseFile` 通过；非 ASCII 行仍为原有 10 行（26/42/43/45/378/993/1248/1249/1253/1254，均为历史注释与 catch 文案），本次新增零非 ASCII；`[Action[int,string]]` 与 `Form.Invoke(Delegate, object[])` 重载已用临时脚本实测可用（临时脚本已删）。GUI 未在本机实跑（会弹窗）。
