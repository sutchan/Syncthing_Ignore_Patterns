# 2026-09-24

## 全量更新项目文档（对齐 v1.26.0）
- 背景：会话间隙项目已提交 `8126032 feat(app): 扫描支持局域网路径与映射盘符，更新至 v1.26.0`（六处单一来源一致为 v1.26.0；文档此前停在 v1.25.3 之前，出现脱节）。
- 修正脱节点：
  - `docs/specs/stignore-gui-flutter/spec.md` REQ-1 输入行 + 行为段补 `listScanDrives`/`normalizeRootPath`/`_resolveRoots`(isDirectorySync)（v1.26.0）；状态里程碑补 v1.25.3 与 v1.26.0；PROP-3「现状」基线刷新为「固定+映射网络驱动器 / 单目录 / UNC」。
  - `docs/project.md` §9.1 模块表 `platform_io` 与 `scan_flow` 两行补 v1.26.0 扫描根目录改进。
  - `README.md` / `README_EN.md` Flutter 段扫描行补「空根目录覆盖固定+映射网络驱动器，UNC 可直填（v1.26.0）」。
  - `docs/development-tasks.md`（进度文档）重写：保留「剩余任务=无」，新增「版本说明」(Flutter v1.26.0 / PS v1.18.5 / 规则集 v1.18.5) 与「后续方向（评估中）」指针（链接 spec.md 改进建议 P0/P1/P2 摘要）。
- 校验：根 `CHANGELOG.md` 与 `project.md` §7 双副本顶端均为 v1.26.0 一致；`固定驱动器` 残留仅出现在 PowerShell 遗留版规格与 v1.26.0 历史条目（均正确保留）；测试文件仍为 19 个（§9.3 数字有效）。
- 未 bump 版本：本次为文档对齐已发布 v1.26.0（同 `555b707 docs:` 既有模式，不另起版本）。如需独立 docs 版本可再 bump。
- MEMORY.md 实际仅 31 行、已精简完整（此前「截断」为注入快照假象），仅修正任务文档约定版本号。
