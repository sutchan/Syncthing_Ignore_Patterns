# 2026-09-23

## 排查 Ruleset drift 告警（结论：当前无漂移，告警为旧产物）
- 用户贴出 CI `validate` 的 "Ruleset copies differ" 警告：根 `.stignore` 被报缺 7 条 AI 助手目录规则（`.codex/.gemini/.qwen/.trae/.opencode/.qoder/.workbuddy`），而 `app/assets/.stignore` 有。
- 用 node 精确比对（UTF-8 + CRLF 归一化，规避 PS5.1 编码坑）：两份文件 **完全一致**（397 行、均 `//Version: 1.18.5`、均含那 7 条规则）。`git diff -- .stignore app/assets/.stignore` 为空（均 == HEAD，且不在 `git status` M 列表）。
- 结论：告警是旧 CI 运行产物（早于根 `.stignore` 补上这 7 条规则，或来自某分支），当前仓库状态无漂移；该检查本就是非阻断 `::warning`，从不 fail 构建。
- 后续可选（用户未明确要改，未执行）：按 `ci.yml` 注释「差异消除后可改为阻断」，将 drift 检查由告警改 `exit 1` 阻断 + 标记 `docs/development-tasks.md` §D 完成（CI 配置变更，按项目规则通常升 PATCH，但 `.stignore` 内容未变）。若用户在某分支仍见此告警，则该分支 root `.stignore` 缺这 7 条，需把 bundle 的 7 条补回 root（保留有用规则、root 保持权威源）。
- 注意：当前工作树另有大量**未提交**改动（`app/lib/i18n.dart`、`services/applier.dart`、`services/scanner.dart`、`test/applier_test.dart`、`test/scanner_test.dart`、README/VERSION/pubspec 等，疑似 v1.19.0 开发），与本任务无关；未改动/未提交它们。

## 扫描/替换跳过应用自身目录（v1.19.1，本日上午）
- 详见 MEMORY.md「扫描/替换跳过应用自身目录」条。`scanner.dart` 始终跳过 `p.dirname(Platform.resolvedExecutable)` + `applier.dart` `skipRoots` 兜底；`flutter test` 13/13。

## 扫描深度 + 大目录过滤（v1.20.0，本日）
- 用户要求：扫描深度可调（默认 3 级，1–10）；大目录过滤（默认跳过 >100 文件的子目录，可开闭、调值）。
- 实现：`services/scanner.dart` 的 `findStignoreFilesRaw` 改为 `async`，新增 `maxDepth`(默认3, 根=1级)/`skipLargeDirs`(默认true)/`maxFilesPerDir`(默认100)；大目录用 `Directory(dir).list().take(threshold+1).length` 流式提前判定（避免 `node_modules` 之类巨目录 `listSync` 卡死），不可读目录按「大」跳过。`app_state` 对应 `_maxDepth`/`_filterLargeDirs`/`_maxFilesPerDir`（**会话内、未持久化**），`scan()` 传参；`home_page` 新增 `_ScanOptions` 卡片（深度 Slider 1–10、阈值 Slider 10–1000）；`i18n` 增 `scanDepth`/`skipLargeDirs`/`maxFilesPerDir`/`level`。`findStignoreFiles` 现返回 `Future`，测试须 `await`。
- 验证：本机 `flutter analyze` 0 问题、`flutter test` **15/15**（新增 maxDepth + skipLargeDirs 2 项）。
- 版本：新功能 → MINOR 升 **v1.20.0**，同步 6 处 Flutter 轨 + 根 CHANGELOG + `docs/project.md` §3/§4/§7/§8/§9.4/§9.5 + `docs/development-tasks.md`。三轨：Flutter 1.20.0 / PowerShell 1.18.5 / 规则集 1.18.5 一致。
- 注：`pubspec.lock` 里的 `1.19.1` 是 `collection` 依赖版本，非本应用（`flutter pub get --offline` 已刷新）；`app/_verify.log`/`_pubget.log` 被 `.gitignore` 覆盖。

## 文档一致性修正 + 升版至 v1.20.1（"更新项目文档" 任务）
- 起手实查：仓库已被并发会话推到 **v1.20.0**（HEAD `da3198f`），三轨一致——Flutter 1.20.0 / PowerShell 1.18.5 / 规则集 1.18.5。`docs/project.md` §7 已含 v1.19.0/1.19.1/1.20.0，多数"当前版本"引用已是 1.20.0，但**规格与模块描述滞后于已实现的代码行为**：
  - Flutter 规格 `REQ-1` 仍写「每根目录 isolate 并行 + `listSync` + 排除 .git/规则源」，未反映 v1.19.1（跳过 exe 目录子树 `skipRoots`/`p.dirname(Platform.resolvedExecutable)`）与 v1.20.0（异步遍历 + `maxDepth`/`skipLargeDirs`/`maxFilesPerDir` 流式计数）；
  - `REQ-2` 缺 `skipRoots`；`REQ-3`/`REQ-5` 仍写「右上角独立下拉 + 语言为会话内状态未持久化」，未反映 v1.19.0（齿轮按钮设置对话框 + `settings_store.dart` 磁盘持久化）；
  - `docs/project.md` §9.1 `scanner.dart`/`applier.dart` 行描述同样滞后。
- 修正：更新 Flutter 规格 `REQ-1/2/3/5` 与 §9.1 两行，对齐 v1.19.0/1.19.1/1.20.0 真实行为。
- 按版本规则（每次修改升 PATCH）升 **v1.20.1**，同步 6 处 Flutter 轨单一来源（VERSION/pubspec `1.20.1+1`/`AppState.version`/`manifest.dart` 示例/README 双语文档徽章+正文）+ 文档内当前版本引用（project.md §3/§4/§8/§9.4/§9.5、dev-tasks ×3）+ 双 CHANGELOG（根 + project.md §7）。
- 核验：6 单一来源均 1.20.1；残留 `1.20.0` 仅历史小节标题/特性归因；三轨一致。改动 10 文件未提交（按惯例用户本地提交）。

## 减小 flutter_windows.dll 体积（咨询，本日下午）
- 事实：`flutter_windows.dll` 是 Flutter SDK **预编译引擎**（Skia/Impeller + Dart 运行时 + D3D 后端），正常 `flutter build windows` 无法改其内容/体积。
- 正常构建无法缩小；唯一真能减体积的路径是**自定义引擎编译**：`gn` 参数 `optimize_for_size=true` + 关闭非 Windows 后端 `enable_gl=false`/`enable_vulkan=false`/`enable_metal=false` + `enable_skia=false`（仅留 Impeller）+ `dart_runtime_mode="release"`/`symbol_level=0`，再 `flutter build windows --local-engine=windows_release --local-engine-src-path=<engine src>`。
- 取舍：自定义引擎需 depot_tools、编译耗时、且破坏 CI 可复现（CI 须自编译引擎），对单体小工具通常不划算。
- 更"规范"的折中：接受引擎体积，靠 §9.5 的 zip 高压缩（SmallestSize/7z -mx9）+ 上轮 `--tree-shake-icons` 减小资源；分发层 UPX 压 dll 有运行期/杀软风险，不推荐。

## 实施构建物体积优化（v1.20.2，本日下午）
- 用户要求"继续优化所有交付构建物体积，尽可能最小交付"。落地 Tier-1（零风险、保持 `.zip` 命名规范 §9.5）：
  - `ci.yml` 构建步加 `--tree-shake-icons`（仅打包用到的 Material 图标字形，home_page 用 14 处 `Icons.*`）；
  - `ci.yml` 打包步弃用 `Compress-Archive`，改 .NET `System.IO.Compression.ZipArchive` + `CompressionLevel.SmallestSize` 高压缩，并剔除 `*.pdb`/`*.exp`/`*.lib` 调试符号；zip 内相对路径用 `[System.IO.Path]::GetFullPath($src)` 解析绝对基址后 `Substring($base.Length+1)` 计算（绕过相对基址导致条目名错乱的坑）。本机无法跑（无外网 pub get / build 交 CI）。
  - 文档：`docs/project.md` §9.2（构建命令）+ §9.5（压缩说明）补充；双 CHANGELOG 加 v1.20.2 小节。
  - 版本：配置/CI 变更 → PATCH 升 **v1.20.2**（HEAD 当时 1.20.1，`cat VERSION`+`git log` 实查）。同步 6 处 Flutter 轨单一来源（VERSION/pubspec `1.20.2+1`/`AppState.version`/`manifest` 示例/README 双语文档徽章+正文）+ 文档当前版本引用 + 双 CHANGELOG。`project.md` §7 与 CHANGELOG 内 `v1.20.1` 历史小节保留不误改（replace_all 仅用于安全处）。
- 预期收益：图标树摇减小资源字体；SmallestSize 较默认 Deflate 再降约 10–30% zip 体积；剔 PDB 进一步减小。最大头 `flutter_windows.dll`/ICU 未变（普通构建不可缩，需自定义引擎，见上节）。
- 未做（待用户裁决）：7z-LZMA（违反 §9.5 须改约定）、自定义引擎裁剪。

## 修复 CI 打包步骤崩溃（v1.20.3，本日下午）
- CI `build-windows` 打包步骤报错：`[System.IO.Compression.ZipArchive] does not contain a method named 'CreateEntryFromFile'`——`CreateEntryFromFile` 实为 `System.IO.Compression.ZipFileExtensions` 的**扩展方法**，PowerShell 不能作为 `ZipArchive` 的静态成员调用（v1.20.2 引入的 bug）。
- 修复：改用**实例方法** `$za.CreateEntry($rel, $level)` 建条目 + `$entry.Open()` / `[System.IO.File]::OpenRead` / `Stream.CopyTo` 流拷贝；`ZipArchiveMode` 用显式枚举；保留 SmallestSize + 排除 `*.pdb/*.exp/*.lib`。
- 本地验证（Windows PowerShell 5.1，`__ztest_tmp.ps1` 临时夹具，验后已删）：条目名 `data/app.so`/`flutter_windows.dll`/`syncthing_ignore_gui.exe`（正斜杠、相对路径正确），调试符号已排除，RESULT PASS。
- 坑：本机仅 PS 5.1（无 pwsh）——`CompressionLevel`/`ZipArchive` 类型名在 PS5.1 需 `Add-Type -AssemblyName System.IO.Compression` 才可见，且 .NET Framework **无 `SmallestSize`**（只能回退 Optimal 验逻辑）；`SmallestSize` 由 CI 的 `shell: pwsh`(PS7/.NET8) 生效，该环境无需 Add-Type（v1.20.2 运行已证 `ZipArchive` 类型可解析）。
- 版本：修复已推送的 v1.20.2 → PATCH 升 **v1.20.3**，同步 6 处 Flutter 轨单一来源 + 文档当前版本引用 + 双 CHANGELOG（v1.20.2 历史小节保留）。

## 修复 CI 产物「二次压缩」（v1.20.4，本日下午）
- 现象：`upload-artifact` 会给上传内容再包一层 zip，此前上传已压缩的 `SyncthingIgnoreGUI-vX-windows-x64.zip` → 产物内「zip 套 zip」（下载产物=外层 zip，里层还是我们的 zip）。
- 修复（按用户「第二层使用目录即可」）：`build-windows` 不再产出 zip，新增 `Stage release files` 把运行文件（剔除 `*.pdb/*.exp/*.lib`）拷贝到 `app/build/package` 目录并以**目录**上传；归档改由 `release` 作业新增的 `Package release archive` 步骤压缩（唯一一次压缩），`softprops` 的 `files:` 改为仓库根 `SyncthingIgnoreGUI-v*-windows-x64.zip`。`ci.yml` 头注释、`docs/project.md` §9.2/§9.5 同步更新。
- 影响：分支构建不再产出发布 zip（仅目录产物）；zip 仅在 tag/release 生成。
- 版本：PATCH 升 **v1.20.4**，同步 6 处 Flutter 轨 + 文档当前引用 + 双 CHANGELOG（v1.20.3/1.20.2 历史小节保留）。git status 中 `.codebuddy` 已 clean，说明用户已提交推送 v1.20.3（印证 1.20.4 升级判断正确）。
- 坑（重要）：`[System.IO.Path]::GetFullPath($relativePath)` 按 **.NET 进程 CWD** 解析，可能不等于 PS 的 `Get-Location`（`Set-Location` 不总是同步进程 CWD）——本地测试因此暴露 `Substring` 越界。改用 `(Get-Item -LiteralPath $src).FullName`（经 PS 提供程序解析，稳健）。CI 各 `run` 步骤进程 CWD 与 PS 位置均为仓库根，原本也能跑，但已加固。
- 本地端到端验证（PS5.1，`__ztest2_tmp.ps1`，验后已删）：Staged 3 文件（app.exe/flutter_windows.dll/data/app.so，排除 pdb/exp）→ zip 条目 `app.exe`/`data/app.so`/`flutter_windows.dll`，RESULT PASS。

## 选项不刷新修复 + 窗口大小/位置记忆（v1.21.0，本日）
- 用户两项需求：① 修复「选项点击后不会更新状态」；② 让应用记住窗口大小与位置。
- 诊断：`app_state.dart` 的 `preview`/`force`/`backup` 是**裸字段**，UI 里 `state.preview = v!` 不触发 `notifyListeners` → 复选框不刷新；另 `_RootField` 用 `TextFormField(initialValue:)`，「浏览」选择后不显示。
- 修复：三选项改为 `setPreview`/`setForce`/`setBackup`（统一 notify）；`root_field.dart` 改 `TextEditingController` + 监听 `AppState` 同步。
- 窗口记忆（**无新增依赖**）：`models/window_bounds.dart`（`WindowBounds` 纯数据 + JSON/可用性校验）+ `services/window_bounds.dart`（win32 `FindWindow('FLUTTER_RUNNER_WIN32_WINDOW')` → `GetWindowRect`/`SetWindowPos`，`_isOnVirtualScreen` 越界忽略）；`AppSettings.window` 持久化；`main.dart` 首帧后 `restoreWindowBounds()` + `startWindowTracking()`（每 2s 采样）；`preferences_state` 管 Timer 并在 dispose 取消。**坑**：ffi 2.x 的 `calloc` 是 `Allocator` 实例 → 须 `calloc.allocate<RECT>(sizeOf<RECT>())`（首版写成 `calloc<RECT>()` 报 `invocation_of_non_function_expression`）。
- 顺带按 ≤200 行规则拆分：`app_state.dart`(326)→7 个 mixin（`state/` 下，互相用 `on <多 mixin>` 约束访问；`version`/`appDirectory` 须加 `@override` 否则 `annotate_overrides` info）；`home_page.dart`(415)→装配壳 + 7 个 `ui/` 子模块，并为主要容器/控件加语义化 `Key`。拆分后最大文件 171 行（scanner.dart）。
- 验证：本机 `flutter pub get --offline` + `flutter analyze` 0 问题 + `flutter test` **19/19**（新增「选项点击生效」回归 + 窗口几何往返/损坏回退/可用性）。
- 版本：新功能 → MINOR 升 **v1.21.0**；同步 6 处 Flutter 轨单一来源 + `docs/project.md`（§3/§7/§8/§9.1/§9.4/§9.5）+ `docs/specs/stignore-gui-flutter/spec.md`（新增 REQ-7 窗口、REQ-3/REQ-6/状态更新）+ 双 CHANGELOG + `docs/development-tasks.md`。未提交（按惯例用户本地提交）。
