# 2026-09-23

## 项目改进建议写入规范文档（v1.25.3）
- 分析了 Flutter 主实现（app/lib），将功能与 UI 完善建议写入 `docs/specs/stignore-gui-flutter/spec.md` 新增「改进建议（评估中）」一节，按 P0/P1/P2 分级，均为基于真实代码事实的缺口：
  - P0：① 扫描中途停止无效（`scanRoots` 无 `isCancelled`，`scan_flow.scan()` 仅扫描后检查 `cancelled`）；② 结果列表写死 160px、无搜索/计数/复制/多选；③ 仅支持单扫描根（拖拽也只替换单根）。
  - P1：备份 UI 恢复、扫描后预检一致状态（✓/✗）、复用未用 i18n 键 `open` 做清单「打开」按钮、启动自动检查更新、设置项扩充。
  - P2：主容器语义化 id、快捷键、规则更新差异摘要、日志导出、进度 ETA。
- 同步 bump 至 v1.25.3（patch）：VERSION / pubspec `1.25.3+1` / `AppState.version` / `manifest.dart` 示例 / README* 徽章 / project.md §3·§9.4·§7 / 根 CHANGELOG + project.md §7 双副本均更新；已校验六处单一来源一致。

## 清理任务文档 + 文档版本一致性补遗（同一 v1.25.3 发布内）
- `docs/development-tasks.md` 已合规：剩余任务=无（v1.25.1 起已完成项已移除），无已完成任务可删；仅其 `版本说明` 仍写 v1.25.2（上一轮 bump 漏改），已修正为 v1.25.3。
- 全仓排查「当前版本」展示位，补修上一轮漏改的 v1.25.2 残留：README.md/README_EN.md 第24行「当前版本说明」与第97行「主实现标题」、project.md §4 示例 `1.25.2+1`、§9.5 命名示例 `v1.25.2`；均改为 v1.25.3。
- 两处 v1.25.3 CHANGELOG（根 CHANGELOG.md + project.md §7）各补一行「docs: 修正 development-tasks.md 版本说明与 project.md 示例版本引用至 v1.25.3」。
- 复检确认：排除 CHANGELOG 历史小节 / spec.md 里程碑 / project.md §7 历史条目 / 记忆目录后，无残留 v1.25.2 当前版本展示位。

## 发布归档含项目命名顶层目录（同一 v1.25.3 发布内）
- 需求：让构建压缩包解压后含一个以项目/产品命名的顶层目录，而非文件散落根目录。
- 实现：仅改 `.github/workflows/ci.yml` 的 `release` 作业 `Package release archive` 步骤——新增 `$innerDir = "${{ env.APP_NAME }}"`（`SyncthingIgnoreGUI`），所有 zip 条目路径加 `APP_NAME/` 前缀；`build-windows` 仍以目录形式上传产物（`app/build/package`），不受影响；归档文件名 `SyncthingIgnoreGUI-v<ver>-windows-x64.zip` 不变，`Create release` 的 `files: ${{ env.APP_NAME }}-v*-windows-x64.zip` 通配仍匹配。
- 文档同步：`docs/project.md` §9.5 补「归档内以产品命名目录 `SyncthingIgnoreGUI/` 为顶层」说明；根 CHANGELOG.md 与 project.md §7 的 v1.25.3 条目各补 `### 构建` / chore(ci) 一行。
- 未另起 bump：v1.25.3 仍为未发布工作集，CI 打包变更并入同版本。如需独立 v1.25.4 可再 bump。

## 精简 development-tasks.md（仅留未完成任务）
- 用户要求「精简文档，只保留未完成的任务在清单里」：`docs/development-tasks.md` 移除「验证边界（非待办）」与「版本说明」两段，仅保留标题 + 单行指针 + `## 剩余任务: **无。**`。
- 验证边界（原生拖放/替换重启须真实 Windows、Inno Setup 未采用）信息已在 MEMORY.md「已实现功能」条目留存，未丢失。
- 顺带整理超长 MEMORY.md：合并/去重、修正版本漂移 v1.25.2→v1.25.3（已 `cat VERSION`+pubspec 实查=1.25.3），压缩为用户偏好/项目约定/环境约束 三节，无信息损失。
